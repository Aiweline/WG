# WG 客户端设计 QA

## 核对范围

- 设计基准：
  - `docs/ui-prototypes/wg-client-01-connection.png`
  - `docs/ui-prototypes/wg-client-02-smart-routing.png`
  - `docs/ui-prototypes/wg-client-03-private-dns.png`
  - `docs/ui-prototypes/wg-client-04-health-update.png`
  - `docs/ui-prototypes/wg-client-05-pairing-wizard.png`
- 实现截图：
  - `ui/client/qa-screenshots/01-connection.png`
  - `ui/client/qa-screenshots/02-smart-routing-final.png`
  - `ui/client/qa-screenshots/03-private-dns.png`
  - `ui/client/qa-screenshots/04-health-update.png`
  - `ui/client/qa-screenshots/05-pairing.png`
  - `ui/client/qa-screenshots/06-mobile-390x844.png`
- 桌面视口：1487 × 1058。
- 移动视口：390 × 844。
- 视觉截图状态：安全演示模式；最终交互复验状态：回环安全开发核心在线。两种状态均未修改系统网络、路由或 DNS。

## 全页对照

五张设计图与五张实现截图已按相同页面状态成对放入同一次视觉比较。布局、内容顺序、导航结构、卡片尺寸、间距、圆角、边框、阴影、字体层级、色彩和图标风格与设计基准一致。实现使用统一的 Phosphor 线性图标，没有占位图、CSS 绘图或手写 SVG。

以下差异属于协议和产品约束的明确修正，不作为视觉缺陷：

- 顶栏增加“安全演示”标识，明确当前没有操作系统网络配置。
- 侧栏增加“首次配对”入口，使五步配对流程可直接进入。
- 健康页将版本拆为客户端包、核心、UI、脚本，避免把不同版本混成一个值。
- 配对页按工程约束使用 BLAKE2s 指纹前缀 `wgs-`，并严格校验文件名 `wg-pairing.wgp`。
- 原型中的旧端口占位已按规范统一为 WG 默认数据端口 `9518`；实时配对摘要和连接端点都保留该端口。

## 聚焦区域与状态

- 连接：已验证断开、连接、重连、降级 DNS 提示、上传/下载和运行时长展示。
- 智能分流：已验证搜索、结果/来源/目标类型筛选、新增、编辑入口、删除；删除纳管规则后目标恢复为 `AUTO`。规则来源字段只读。
- 私有 DNS：系统 DNS 快照只读；刷新仅更新应用内副本；诊断结果明确系统 DNS 未被修改。
- 健康与更新：已验证健康检查、检查更新、升级和回滚的演示状态；四类版本独立展示。
- 首次配对：已验证从步骤 1 开始、IPv4/IPv6、文件名、后端签发的单次 `validation_id`、后端指纹与端口、独立指纹确认、最小授权和完成状态；配对输入变化会使旧校验失效，授权范围不含系统 DNS。
- 键盘与可访问性：核心控件均使用语义化按钮、输入框和标签，具备可见焦点状态；状态色与正文对比度可读。

## 响应式核对

- 390 × 844 下页面宽度与视口一致，无页面级横向溢出。
- 内容区按单列重排，文字和操作按钮未重叠、裁切或挤压。
- 侧栏导航在窄屏转为可横向滚动的导航条，主要功能仍可访问。

## 比较与修正记录

1. 第一轮发现智能分流筛选项只显示“全部”，上下文不够清楚，定为 P2 内容/可扫描性问题。
2. 已改为“结果：全部”“来源：全部”“目标类型：全部”，并用 `02-smart-routing-final.png` 重新核对。
3. 修正后未发现剩余 P0、P1 或 P2 级视觉、行为、可访问性问题。
4. 可接受的 P3 差异：实现中的线性图标略轻，以及安全演示标识和配对快捷入口属于额外产品状态说明。

## 工程验证

- 浏览器控制台：无 error 或 warning。
- ESLint：通过。
- Vitest：2 个测试文件、16 个测试全部通过。
- Vite 生产构建：通过。
- API 合同覆盖：状态、规则、DNS、诊断、更新、回滚、配对；后端返回 501 时不会伪造成功状态。
- Go UI 主机与安全开发核心已通过真实回环接口联调：客户端能识别“后台在线”并完成模拟重连。
- 规则新增已验证“1 小时”会转换为绝对到期时间并由后台接受；删除同一规则后返回 `AUTO` 与“自动学习”，不会误改为 `DIRECT`。
- 规则编辑已验证目标只读并携带 revision；后台拒绝写入时抽屉保持打开，live 模式不会伪造本地成功状态。
- 后端规则过期会真正从决策引擎移除；按 ID 与按规范化 target 删除都已覆盖，脚本 `split delete TARGET` 与 UI 共用同一状态。
- UI 与 `wg-client pair --execute` 均已通过真实配对契约复验：先校验并取得单次 validation ID，再逐字比较指纹并登记；端点统一显示规范端口 `9518`。
- 私有 DNS“重新同步”只增加应用内只读 generation；页面和诊断都持续显示系统 DNS 未修改。
- 回滚接口返回 501 时，客户端明确提示没有执行升级或回滚，界面状态保持不变。
- 最新浏览器复验控制台日志为空；配对、诊断、规则增改删和失败关闭均通过真实回环 API。
- `make test` 已通过普通测试、竞态检测、静态检查、前端检查/测试/构建和脚本语法检查。

final result: passed
