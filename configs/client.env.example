# WG client development defaults.
# The management API is deliberately loopback-only. The private DNS component
# may read a system resolver snapshot, but it must never write system DNS.

WG_CLIENT_BIN=bin/wg-core
WG_CLIENT_MANAGEMENT_ADDR=127.0.0.1:47003
